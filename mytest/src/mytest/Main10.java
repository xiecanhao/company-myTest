package mytest;

public class Main10 {
	public static void main(String[] args) {
		Object o = null;
		Class<Object> c = Object.class;
		// for (int i$ = 0; i$ < len$; ++i$) {
		// Annotation an = arr$[i$];
		// if (an instanceof Required) {
		// Required ran = (Required) an;
		// this.requiredArr[i] = ran;
		// } else if (an instanceof Optional) {
		// Optional arg10 = (Optional) an;
		// this.requiredArr[i] = arg10;
		// } else {
		// if (!(an instanceof Attribute)) {
		// }
		// Attribute arg11 = (Attribute) an;
		// this.requiredArr[i] = arg11;
		//
		// throw new IllegalArgumentException(
		// method.getName() + "参数注解类型错误");
		// }
		// }
	}
}
